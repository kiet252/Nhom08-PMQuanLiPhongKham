<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#E5E7EB">

    <!-- Khu vực Camera (Mở rộng tràn toàn bộ màn hình phía sau) -->
    <FrameLayout
        android:id="@+id/camera_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="#000000">

        <androidx.camera.view.PreviewView
            android:id="@+id/camera_preview"
            android:layout_width="match_parent"
            android:layout_height="match_parent" >

            <View
                android:layout_width="207dp"
                android:layout_height="276dp"
                android:layout_gravity="center"
                android:layout_marginBottom="60dp"
                android:background="@drawable/bg_scan_frame" />
        </androidx.camera.view.PreviewView>

        <ImageButton
            android:id="@+id/btnBackAdminExDetail"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="start|top"
            android:layout_marginStart="16dp"
            android:layout_marginTop="16dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Quay lại"
            android:src="@drawable/ic_arrow_back"
            app:tint="#FFFFFF" />

        <!-- Khung định vị khuôn mặt -->
        <!-- Đẩy lên một chút cho cân đối với nút Quét ở dưới -->

        <!-- Thông báo trạng thái / Hướng dẫn ngắn -->
        <TextView
            android:id="@+id/tv_guidance"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|center_horizontal"
            android:layout_marginBottom="140dp"
            android:background="@drawable/bg_status_pill"
            android:paddingHorizontal="20dp"
            android:paddingVertical="10dp"
            android:text="Hãy nhìn thẳng vào camera, giữ yên 1–2 giây.\nĐảm bảo đủ sáng và mặt nằm trong khung."
            android:textAlignment="center"
            android:textColor="#FFFFFF"
            android:textSize="14sp" />
    </FrameLayout>

    <!-- Khu vực Thông tin (Giờ chỉ còn khung trắng ôm quanh nút Quét) -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/bottom_container"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_bottom_sheet"
        android:elevation="16dp"
        android:padding="24dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <Button
            android:id="@+id/btn_confirm"
            android:layout_width="0dp"
            android:layout_height="60dp"
            android:backgroundTint="#0D3F6E"
            android:text="Quét"
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:textStyle="bold"
            app:cornerRadius="16dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>